overwrite: true
schema: https://api-mumbai.lens.dev
documents: "**/*.graphql"
hooks:
  afterOneFileWrite:
    - eslint --fix
    - prettier --write
generates:
  src/types/lens.ts:
    plugins:
        - typescript
        - typescript-operations
        - fragment-matcher
        - typescript-react-apollo
