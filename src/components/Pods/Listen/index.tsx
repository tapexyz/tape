import Layout from "@components/wrappers/Layout";

import PodPlayer from "../PodPlayer";

const Listen = () => {
  return (
    <Layout>
      <PodPlayer />
    </Layout>
  );
};

export default Listen;
