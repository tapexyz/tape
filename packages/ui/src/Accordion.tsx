import * as AccordionPrimitive from "@radix-ui/react-accordion";

export const { Accordion } = AccordionPrimitive;
export const { AccordionItem } = AccordionPrimitive;
export const { AccordionTrigger } = AccordionPrimitive;
export const { AccordionContent } = AccordionPrimitive;
